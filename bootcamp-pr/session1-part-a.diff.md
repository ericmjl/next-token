# Diff for session1-ai-foundations-responsible-ai.md

Apply these two edits on branch `eric/session1-part-a-explainer`.

## Edit 1: under `## Part A — How AI actually works (intuition, math-light)`

After the existing `_TODO_` line, add:

```markdown
**Teaching instrument: [next-token](https://github.com/ericmjl/next-token)**, an
interactive scrolling explainer built for this session. Ten sections, each with a
live demo: next-letter prediction with a tiny model that trains at page load,
tokens, temperature and sampling, training, the interpolation insight, a
spot-the-fabricated-citation exercise, scale, and a bridge to foundation
models. Deploy notes in [materials/part-a/README.md](materials/part-a/README.md).
```

## Edit 2: under `## Materials`

Add:

```markdown
- Interactive: [next-token](https://github.com/ericmjl/next-token), the Part A
  explainer (runs in the browser; source ships as one container)
```
