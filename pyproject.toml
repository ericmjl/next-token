[project]
name = "next-token"
version = "0.1.0"
description = "An interactive, teachable explainer of how LLMs actually work, built for the UMass Chan AI for Bioinformatics bootcamp."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "jinja2>=3.1",
    "tiktoken>=0.7",
]

[tool.uv]
package = false
